apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: store-platform
  labels:
    app: frontend
    component: ui
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: frontend
